# Nuxt Button

This wrapper proves that Design Lab renders a real `UButton` from `@nuxt/ui` through the Vue runtime adapter. It preserves a small product-facing contract instead of exposing every Nuxt UI option.

Use `variant` for action emphasis, `size` for stable control geometry, and `loading` only while the action is pending.
