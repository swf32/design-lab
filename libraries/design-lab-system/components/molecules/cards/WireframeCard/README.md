# Wireframe Card

Catalog identity for a discovered page-level Wireframe. The card exposes review lifecycle plus layout, saved-state, and transition counts without pretending to render the real Wireframe.

Its miniature is an illustrative structural specimen and never imports the actual renderer. Use `selected` only when the route continues to show the catalog; fullscreen review routes normally replace the module shell.
