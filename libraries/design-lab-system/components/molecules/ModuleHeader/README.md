# Module Header

Shared identity header for module indexes and component workbenches. It combines an eyebrow, title, and one contextual trailing role: count, source metadata, or actions.

Workbench back navigation is a context story because it verifies the header inside the component-detail flow.
