import type { ReactNode } from 'react'
export function ModuleHeader({ eyebrow, title, count, backLabel, onBack, meta, actions }: { eyebrow:string; title:string; count?:number; backLabel?:string; onBack?:()=>void; meta?:string; actions?:ReactNode }) { return <header className={`dl-module-header${onBack?' dl-module-header--with-back':''}`}>{onBack && <button type="button" className="dl-module-header__back" onClick={onBack}>← {backLabel ?? 'Back'}</button>}<div className="dl-module-header__identity"><span>{eyebrow}</span><h1>{title}</h1></div>{meta && <code>{meta}</code>}{count !== undefined && <b>{count}</b>}{actions && <div className="dl-module-header__actions">{actions}</div>}</header> }
