# Color Picker

Compact color override control with a custom trigger, native spectrum input, preset swatches, editable HEX value, and nullable reset.

Use a controlled `value` when color belongs to another entity. `null` means that the consumer's semantic default remains active. The popover closes on outside pointer interaction or Escape and keeps every color choice accessible by name.
