import type { ButtonHTMLAttributes, ReactNode } from 'react'
export type ButtonProps = ButtonHTMLAttributes<HTMLButtonElement> & { variant?: 'primary' | 'secondary' | 'ghost' | 'danger'; size?: 'small' | 'medium' | 'large'; loading?: boolean; fullWidth?: boolean; leading?: ReactNode; trailing?: ReactNode; children: ReactNode }
export function Button({ variant = 'secondary', size = 'medium', loading = false, fullWidth = false, leading, trailing, className = '', children, disabled, ...props }: ButtonProps) { return <button className={`dl-button dl-button--${variant} dl-button--${size}${fullWidth ? ' dl-button--full' : ''}${loading ? ' dl-button--loading' : ''}${className ? ` ${className}` : ''}`} disabled={disabled || loading} aria-busy={loading || undefined} {...props}>{loading ? <span className="dl-button__spinner"/> : leading}<span>{children}</span>{!loading && trailing}</button> }
