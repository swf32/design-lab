# Workspace Header

Persistent shell header for Design Lab. It keeps product and active-module identity stable while
accepting the mobile navigation trigger and global utilities as explicit slots.

The header owns responsive spacing and action hit areas. Content modules use `ModuleHeader`
instead; product Pages and Wireframes provide their own navigation through their Library.
