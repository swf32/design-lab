{
  "id": "user-flow-canvas",
  "name": "User Flow Canvas",
  "status": "ready",
  "description": "Infinite pannable and pinch-zoomable state-transition graph whose collision-safe nodes pair real desktop and mobile Wireframe screens.",
  "aliases": ["flow graph", "state graph", "journey canvas"],
  "useWhen": [
    "A Wireframe or Page review needs to explain which user action causes each state transition."
  ],
  "avoidWhen": ["Content is a linear checklist or static hierarchy without state transitions."],
  "entry": "UserFlowCanvas.tsx",
  "preview": "UserFlowCanvas.preview.tsx",
  "stories": "UserFlowCanvas.stories.tsx",
  "docs": "README.md",
  "changelog": "CHANGELOG.md",
  "variants": ["grid"],
  "states": ["default", "selected", "panning"],
  "props": {
    "nodes": { "type": "UserFlowCanvasNode[]", "required": true },
    "edges": { "type": "UserFlowCanvasEdge[]", "required": true },
    "selectedId": { "type": "string | null", "required": true },
    "onSelect": { "type": "(id: string) => void", "required": true },
    "onPreview": { "type": "(id: string) => void", "required": false }
  }
}
