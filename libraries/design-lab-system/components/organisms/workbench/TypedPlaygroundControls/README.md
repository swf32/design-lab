# Typed Playground Controls

Generic renderer for the serializable Playground control registry. It selects the production Input,
Checkbox, Select, Slider, Radio Button, or Color Picker from each control definition and never
branches on a particular Component implementation.
