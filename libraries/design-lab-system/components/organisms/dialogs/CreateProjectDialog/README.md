# Create Project Dialog

Design Lab onboarding dialog for connecting an existing source or starting clean. It owns the
interactive presentation and review summary while the application supplies filesystem scanning and
confirmed creation callbacks. The Component depends only on the stable summary shape, not app API
modules.
