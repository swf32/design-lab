# SectionHeader — changelog

## 2026-05-25 21:40 — h1-to-24px

- What: level=1 heading shrunk from `--font-size-32` to `--font-size-24`.
- Why: the design lead asked for a uniform 24px page-title size across every route;
  pairs with eyebrow + description being stripped at the callsites.
