{
  "id": "button",
  "name": "Button",
  "status": "ready",
  "entry": "Button.tsx",
  "preview": "Button.preview.tsx",
  "stories": "Button.stories.tsx",
  "docs": "README.md",
  "changelog": "CHANGELOG.md",
  "variants": ["primary", "secondary"],
  "states": ["default", "hover", "focus", "disabled"],
  "description": "Triggers the primary or secondary action in Northstar travel flows.",
  "aliases": ["action", "cta"],
  "useWhen": ["submit a travel form", "choose a journey", "confirm a clear user action"],
  "avoidWhen": ["navigate between pages without an action", "toggle persistent state"],
  "props": {
    "variant": { "type": "\"primary\" | \"secondary\"", "default": "primary" },
    "fullWidth": { "type": "boolean", "default": false },
    "disabled": { "type": "boolean", "default": false }
  }
}
